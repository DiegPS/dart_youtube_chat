// ignore: unused_import
import 'package:dart_youtube_chat/dart_youtube_chat.dart';

void main() async {
  // Example: connect to a YouTube live by handle.
  // Start polling (fetches the live page first, then polls every 1s).
  // Uncomment to run against a real live stream:
  // final chat = LiveChat(id: const YoutubeId(handle: 'MrBeast'));
  // await chat.start();
  // chat.messages.listen((item) {
  //   print('[${item.author.name}] ${item.message.map((m) => m.text).join()}');
  // });

  print('dart_youtube_chat example — ready.');
}
